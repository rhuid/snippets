# -*- mode: snippet -*-
# name: date (choose format)
# key: dd
# --
`(format-time-string (cdr (assoc (yas-choose-value (rh/date-format-candidates))
                                 (rh/date-format-candidates))))`